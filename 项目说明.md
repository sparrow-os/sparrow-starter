# Sparrow Starter 项目分析

> 本文是对 `sparrow-starter` 模块源码的通读分析与总结，生成于 2026-09-09。
> 分析对象：`src/main/java` 下全部 44 个 Java 源文件及 `src/main/resources` 自动装配清单。

---

## 1. 项目概述

`sparrow-starter` 是 **Sparrowzoo 自研框架的 Spring Boot 集成层（Starter）**。它本身不承载业务逻辑，职责是把 sparrow 生态（`sparrow` / `sparrow-orm` / `sparrow-data-source` / `sparrow-container` / `sparrow-protocol-mq` 等）的组件，在 **Spring Boot 3 / JDK 18** 环境下自动装配成 Bean，业务项目引入本依赖后即可直接使用。

```
业务应用 ──► sparrow-starter ──┬─► sparrow / sparrow-container / sparrow-protocol-mq
                               ├─► sparrow-orm / sparrow-data-source  (optional)
                               ├─► druid-spring-boot-starter          (optional)
                               ├─► mybatis-spring-boot-starter        (optional)
                               └─► spring-boot-starter-data-redis / thymeleaf (optional)
```

绝大多数依赖声明为 `optional=true`，配合自动装配中的 `@ConditionalOnClass`，实现「**引入什么依赖、就启用什么能力**」的插件式特性。

## 2. 技术栈

| 维度 | 内容 |
| --- | --- |
| 构建 | Maven，parent 为 `com.sparrowzoo:sparrow-bom:1.0.5-SNAPSHOT` |
| 运行时 | JDK 18 / Spring Boot 3（Servlet、jakarta 命名空间） |
| 生态 | sparrow 系列（core/orm/data-source/container/protocol-mq） |
| 第三方 | Druid、MyBatis、Redis(Lettuce)、Thymeleaf、AspectJ、RoaringBitmap、UserAgentUtils、jakarta.inject |

## 3. 模块结构与职责

### 3.1 配置中心 `com.sparrow.spring.config`

- `SparrowConfig`：`sparrow.*` 前缀的统一配置，通过 `@EnableConfigurationProperties` 注入，含五组子配置：
  - `cors`：跨域开关与白名单
  - `mvc`：模板引擎、ajax 匹配规则、上传/静态资源路径、登录/错误页等 Web 常量
  - `email`：邮件服务开关与连接信息
  - `datasource`：默认 schema、密码环境变量 key、调试开关
  - `enums`：需要暴露给前端的业务/编码枚举类清单

### 3.2 容器层 `com.sparrow.spring.container`（本模块最核心的设计）

| 类 | 职责 |
| --- | --- |
| `SpringContext` | 静态持有 `ApplicationContext` 的桥，以 `HIGHEST_PRECEDENCE` 注册，供无注入点的场景取 Bean |
| `SpringContainer` | 继承 sparrow 原生 `SparrowContainer`。**双容器策略**：先查本地 singleton 注册表，miss 后回落 Spring Context |
| `SpringServletContainer` | 通过 `RequestContextHolder` 获取 req/resp，适配 sparrow 的 `AbstractServletContainer` |
| `EnumsContainer` | 反射加载枚举类为 `KeyValue<Integer,String>` 列表（配合 `EnumUniqueName` 注解），供前端下拉等场景 |

> **设计意图**：`SpringContainer` 通过 JDK SPI（见 `META-INF/services/com.sparrow.container.Container`）暴露。DDD / 整洁架构的 domain 核心层因此可以**不依赖任何 Spring API**，却仍能读取 Spring 管理的 Bean —— 核心层只依赖 sparrow 容器抽象，由 starter 提供 Spring 适配实现。

### 3.3 自动装配层 `com.sparrow.spring.starter.autoconfiguration`

装配顺序由 `META-INF/spring/...AutoConfiguration.imports` 声明（14 条）。核心清单：

| 装配类 | 启用的能力 | 关键条件 |
| --- | --- | --- |
| `SparrowAutoConfiguration.SpringContextAutoConfiguration` | 注册 `SpringContext` | 无 |
| `SparrowAutoConfiguration` | `IpSupport / Monitor / BeanCopier / WebConfigReader / DatasourceConfigReader / CookieUtility / SpringServletContainer / FlashParamPrepareAspect / FileService / EmailSender / ImageExtractorRegistry` | 默认 Bean 均 `@ConditionalOnMissingBean`；Email 需 `sparrow.email.enabled=true` |
| `RedisAutoConfiguration` | 自定义 `RedisTemplate`（key String / value GenericJackson2Json）、Redis 限流器 `OperateLimiter` | classpath 有 `RedisTemplate`，装配于 Boot 默认 Redis 自动配置**之前** |
| `SparrowDataSourceAutoConfiguration` | sparrow 自研 `ConnectionProxyContainer` + `ConnectionPool`（bean 名 `sparrow_default`） | classpath 有 `ConnectionPool`，且无其他 `DataSource` |
| `DruidDataSourceAutoConfiguration` | 装配 **Druid** `DataSource`（bean 名 `sparrow_default`），密码经环境变量回调解密 | classpath 有 `DruidDataSource`，且无其他 `DataSource` |
| `SparrowORMAutoConfiguration` | sparrow ORM 的 `DataSourceFactory / 事务管理器 / 多数据源 Dispatcher` | classpath 有 `DBORMTemplate`，装配于 Spring 事务自动配置**之前** |
| `SparrowReleaserAutoConfiguration` / `OtherReleaserAutoConfiguration` | 按数据源选型自动补上对应 `ConnectionReleaser` | 依据 sparrow 数据源相关类是否存在互斥补位 |
| `MvcConfigurerAdapter` | `ClientInfoArgumentResolvers` 参数解析器、CORS 映射 | 无 |
| `CaptchaAutoConfiguration` | 验证码 Servlet 注册（`/captcha`），Session 或 Redis 实现 | `sparrow.captcha.service=session\|redis` |
| `TemplateEngineAutoConfiguration` | Thymeleaf 的 viewName 自动映射 | classpath 有 `TemplateEngine` |
| `EnumsAutoConfiguration` | 装配 business / coder 两个 `EnumsContainer` | 配置了 `sparrow.enums.*` |
| `FilterAutoConfiguration` | `FlashFilter / SparrowCorsFilter / ClientInformationFilter / AccessMonitorFilter / SpringGlobalAttributeFilter` | CORS Filter 需 `sparrow.cors.allow=true` |
| `FilterRegistrationBeanAutoConfiguration` | 为 CORS / ClientInformation Filter 注册顺序与路径 | 无 |

> 大量 `@ConditionalOnMissingBean` 意味着业务应用可以**无缝覆盖默认 Bean**，这是可插拔设计的根基。

### 3.4 Web / MVC `com.sparrow.spring.mvc`

- `ControllerReturnAdvice`：`ResponseBodyAdvice`，将非 `Result` 的返回值统一包装为 `Result`（`String` 单独序列化）；`@ExceptionHandler` 区分 **AJAX（返回 Result）** 与 **页面（redirect 到错误页并携带 flash）**。注意：需业务层自行开启 `@ControllerAdvice`。
- `ModelAndViewUtils + FlashFilter + FlashParamPrepareAspect`：自研 **Flash 状态保持机制**（替代 Spring `RedirectAttributes`）：
  1. `FlashParamPrepareAspect`：AOP 拦截 `@RequestMapping` 方法，把入参（POJO / 命名参数）预写入 request attribute；
  2. `ModelAndViewUtils`：重定向前把现场属性 + 上下文写入 session（`FLASH_KEY`）；
  3. `FlashFilter`：请求命中目标 URL 时把 session 中的现场回填到 request；**URL 变化（跨跳转）时清空 session** —— 比 Spring 原版多支持了"中间跳转状态保持"。
- `DefaultController`：`/**` 兜底到模板视图（已 `@Deprecated`）、`favicon.ico` 空响应。
- `message/converter/*`：旧版 VO / List 自定义 JSON 转换器，已 `@Deprecated` 并停用，回归 Spring 默认转换（保留 `ViewObjectUtils` 判断工具）。

### 3.5 Filter 与监控 `com.sparrow.spring.filter(.monitor)`

注册顺序：ClientInformation → CORS → Flash → AccessMonitor → GlobalAttribute。

| Filter | 职责 |
| --- | --- |
| `ClientInformationFilter` | 从 header 收集 AppId / 版本 / 设备 / 经纬度 / IMEI 等，`UserAgentUtils` 解析 OS/浏览器并推断 PC/Mobile，绑定到 `SessionContext` |
| `SparrowCorsFilter` | Filter 级 CORS（早于 Spring 拦截器生效），与 MVC `CorsRegistry` 双通道互补 |
| `FlashFilter` | 见 3.4，flash 现场回填与清除 |
| `AccessMonitorFilter` | 记录每次访问（跳过 favicon），交由 `Monitor` 统计 |
| `SpringGlobalAttributeFilter` | 把 `sparrow.mvc.*` 路径类常量填充为全局 request/session attribute |

- `Monitor`：纯内存监控。`RoaringBitmap` 记录 IP 基数（省内存），`LinkedHashMap + AtomicLong` 记录秒级 QPS（近 10 分钟）、每 IP 访问 TOP、近一周每日访问量。`result()` 汇总成 `MonitorResult`。

### 3.6 拦截器 `com.sparrow.spring.Interceptor`

- `FlashParamPrepareAspect`：见 3.4（包名历史遗留为 `Interceptor`）。
- `MybatisInterceptor`：MyBatis 插件，打印每条 SQL（含参数还原）与执行耗时，仅代理 `Executor`。

### 3.7 持久化 / 数据源 / 中间件

- `RecordStateTypeHandler`：MyBatis `TypeHandler<StatusRecord>`，状态枚举 ↔ 数据库 int。
- `DruidCustomPasswordCallback`：`DruidPasswordCallback` 扩展，密码从**环境变量**（`dataSource.passwordKey`）读取，debug 开关可打印，避免密码落库。
- `AbstractSpringMQHandler`：桥接 sparrow `AbstractMQHandler` 与 Spring 生命周期（`InitializingBean`）。
- Redis（`redis/`）：`OperateLimiter` 接口 + `RedisOperateLimiter` 实现（基于 INCR 的固定窗口/滚动窗口限流、续期），`RedisCaptchaService`（验证码 10 分钟过期）。

### 3.8 Resolver `com.sparrow.spring.resolver`

- `ClientInfoArgumentResolvers`：`HandlerMethodArgumentResolver`，Controller 方法参数声明 `ClientInformation` 时自动注入（数据来自 Filter 绑定的 `SessionContext`）。

### 3.9 测试基建 `com.sparrow.spring.test`

- `TestWithoutBootstrap` 组合注解 + `SparrowTestExecutionListener`：**无 `Application` 启动类也能起 Spring 上下文**跑单测；测试前通过 SPI 初始化 sparrow 原生容器（不扫 Controller / 单例 / 拦截器）。
- 实际用例稀少（当前仅 `MonitorTest` 的 main 方法）。

## 4. 设计亮点小结

1. **可插拔自动装配**：`optional` 依赖 + `@ConditionalOnClass` + 有序 `AutoConfiguration.imports`，引入即启用、不引入即静默。
2. **业务可覆盖**：默认 Bean 几乎全部 `@ConditionalOnMissingBean`。
3. **容器双通道解耦**：SpringContainer 让核心领域零 Spring 依赖却可用 Spring Bean（SPI + 回落策略）。
4. **数据源自动选型**：Druid 与 sparrow 自研连接池互斥装配，并自动配齐连接释放器。
5. **自研 Flash / 统一 Result**：AOP + 过滤器 + 工具类三段式协作，兼顾 AJAX 与多页跳转场景。
6. **轻量监控**：RoaringBitmap 做 IP 基数统计，内存友好。

## 5. 工作区现状（分析时点）

- 工作区存在约 50 项未提交变更，主线为 Spring Boot 3 / JDK 18 **升级适配中的重构**：
  - 大量 `controller → mvc` 包重命名迁移（`DefaultController` / `ControllerReturnAdvice` / `message/converter` 等，对应 git `RM` 记录）；
  - `container / filter / redis / autoconfiguration / monitor / test` 多处同步修改，收敛到 `com.sparrow.spring.*` 结构。
- 最近一次提交信息为乱码（`Öع¹ ´úÂë½ṹ`），疑似提交时终端编码问题导致中文 commit message 损坏。

## 6. 潜在问题观察（未修改，仅记录）

> 以下为源码通读时的疑点，供后续核查，**不代表已确认的缺陷**。

1. `mvc/ControllerReturnAdvice.java` 的 `supports()`：`methodParameter.getParameterType().isAssignableFrom(Result.class)` 方向疑似写反（应为 `Result.class.isAssignableFrom(parameterType)`），可能导致大量非 Result 返回值未被包装，需确认是否符合预期。
2. `TemplateEngineAutoConfiguration.addViewControllers()`：`sparrow.mvc.auto-mapping-view-names` 未配置时为 null，存在 NPE 风险。
3. `pom.xml`：`spring-boot-starter-data-redis` 声明了两次；`RedisAutoConfiguration` 外层类上重复 `@ConditionalOnClass(RedisTemplate.class)`。
4. `DruidDataSourceAutoConfiguration.passwordCallback()` 标注 `@Inject` 但未标 `@Bean`（该方法由 `sparrow_default()` 直接调用，注解无效但无害）。
5. `Monitor` 的 `IP_COUNT_LIMIT=1000` 意味着每 IP 计数维度仅跟踪最近 1000 条，超出会淘汰；做长期精确统计时需确认 `CollectionsUtility.incrementByKey` 的淘汰语义。
